---
// src/pages/index.astro
export const prerender = false; // server mode only

import Layout from '@/layouts/Layout.astro';
import HomeContent from '@/components/HomeContent.astro';

const isApp = Astro.locals.isApp;
---

<Layout isApp={isApp}>
  <HomeContent />
</Layout>
